{
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "runtime": "nodejs20",
      "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run build"]
    },
    {
      "source": "functions-issues",
      "codebase": "issues"
    }
  ],
  "firestore": { "rules": "firestore.rules" },
  "storage":   { "rules": "storage.rules" }
}
