{
  "name": "functions",
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "clean": "rimraf lib",
    "build": "tsc -p .",
    "deploy:farm": "firebase deploy --only functions:createOrJoinFarm,functions:myFarm,functions:harvests"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5"
  }
}
