{
  "name": "liff-nexiiot-src",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "serve": "py -3 -m http.server 8000",
    "test": "echo \"no tests\" && exit 0"
  }
}
